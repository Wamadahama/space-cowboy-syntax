var odds = evens.map(v => v + 1);

class Point extends FOO.bar {
  constructor(x, y){
    this.x = x;
    this.y = y;
  }

  get x(){
    return this.x;
  }

  get y(){
    return this.y;
  }
}

document.querySelector('#id');

let x = document.querySelector('#id');


x.addClass(className);
